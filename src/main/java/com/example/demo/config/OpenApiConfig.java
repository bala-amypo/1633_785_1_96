package com.example.demo.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class OpenApiConfig {
    // Intentionally empty
    // Keeps project structure aligned with screenshots/tests
}
